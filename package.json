{
  "name": "after-the-numbers",
  "version": "3.0.0",
  "private": true,
  "description": "AFTER THE NUMBERS editorial portfolio with case studies and Markdown publishing",
  "scripts": {
    "dev": "node build.js && node scripts/dev-server.js",
    "build": "node build.js",
    "check": "node --check build.js && node --check site-pages.js && node --check script.js && node --check article.js && node --check writing-archive.js && node scripts/validate-build.js",
    "test": "npm run build && npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
