const fs=require("fs"),path=require("path");
function mdInline(s){return String(s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>").replace(/\*(.+?)\*/g,"<em>$1</em>").replace(/`([^`]+)`/g,"<code>$1</code>")}
function mdRender(src){const lines=String(src||"").split(/\r?\n/);let out=[],list=false;const end=()=>{if(list){out.push("</ul>");list=false}};for(const line of lines){if(/^### /.test(line)){end();out.push(`<h3>${mdInline(line.slice(4))}</h3>`)}else if(/^## /.test(line)){end();out.push(`<h2>${mdInline(line.slice(3))}</h2>`)}else if(/^# /.test(line)){end();out.push(`<h1>${mdInline(line.slice(2))}</h1>`)}else if(/^- /.test(line)){if(!list){out.push("<ul>");list=true}out.push(`<li>${mdInline(line.slice(2))}</li>`)}else if(/^> /.test(line)){end();out.push(`<blockquote>${mdInline(line.slice(2))}</blockquote>`)}else if(!line.trim()){end()}else{end();out.push(`<p>${mdInline(line)}</p>`)} }end();return out.join("\n")}const ROOT=__dirname,CONTENT=path.join(ROOT,"content","posts"),OUT=path.join(ROOT,"dist");
const esc=s=>String(s||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]||c));
function cp(s,d){if(!fs.existsSync(s))return;fs.mkdirSync(d,{recursive:true});for(const e of fs.readdirSync(s,{withFileTypes:true})){const a=path.join(s,e.name),b=path.join(d,e.name);e.isDirectory()?cp(a,b):fs.copyFileSync(a,b)}}
function fm(t){const m=t.match(/^---\s*\n([\s\S]*?)\n---\s*\n([\s\S]*)$/);if(!m)return{data:{},body:t};const data={};for(const l of m[1].split(/\r?\n/)){const i=l.indexOf(":");if(i<0)continue;let k=l.slice(0,i).trim(),v=l.slice(i+1).trim();if((v.startsWith("\"")&&v.endsWith("\""))||(v.startsWith("'")&&v.endsWith("'")))v=v.slice(1,-1);if(v==="true")v=true;if(v==="false")v=false;data[k]=v}return{data,body:m[2]}}
function page(p){const cat={work:"WORK",investing:"INVESTING",life:"LIFE"}[p.category]||"WRITING";return `<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(p.title)} · HWANG YUNJIN</title><meta name="description" content="${esc(p.excerpt)}"><style>body{margin:0;background:#f4f0e7;color:#071120;font-family:Arial,sans-serif}.s{max-width:900px;margin:auto;padding:36px 24px 100px}nav{display:flex;justify-content:space-between;border-bottom:1px solid #bbb;padding-bottom:18px;font-size:11px}nav a{color:inherit;text-decoration:none}header{padding:88px 0 46px;border-bottom:1px solid #bbb}h1{font-size:clamp(38px,6vw,68px);line-height:1.15;letter-spacing:-.05em;margin:18px 0}.k{font-size:10px;color:#1a57a8;letter-spacing:.12em}.m{font-size:10px;color:#686865}.b{max-width:690px;margin:58px auto 0;font-size:17px;line-height:1.95}.b h1{display:none}.b h2{font-size:27px;margin-top:60px}.back{display:inline-block;margin-top:70px;color:inherit;text-decoration:none;font-size:11px}</style></head><body><main class="s"><nav><a href="/">HWANG YUNJIN</a><span>WRITING / ${cat}</span></nav><header><span class="k">${esc(p.category_label||cat)}</span><h1>${esc(p.title)}</h1><div class="m">${esc(p.date)} · ${esc(p.read_time||"")}</div></header><article class="b">${mdRender(p.body)}</article><a class="back" href="/#writing">← BACK TO WRITING</a></main></body></html>`}
if(fs.existsSync(OUT))fs.rmSync(OUT,{recursive:true,force:true});fs.mkdirSync(OUT,{recursive:true});for(const f of ["index.html","styles.css","script.js"])fs.copyFileSync(path.join(ROOT,f),path.join(OUT,f));cp(path.join(ROOT,"admin"),path.join(OUT,"admin"));cp(path.join(ROOT,"uploads"),path.join(OUT,"uploads"));const posts=[];for(const file of fs.readdirSync(CONTENT).filter(x=>x.endsWith(".md"))){const {data,body}=fm(fs.readFileSync(path.join(CONTENT,file),"utf8"));if(data.draft===true)continue;const slug=file.replace(/\.md$/,"").replace(/^\d{4}-\d{2}-\d{2}-/,"");const p={...data,body,slug,url:`/writing/${slug}/`};posts.push(p);const d=path.join(OUT,"writing",slug);fs.mkdirSync(d,{recursive:true});fs.writeFileSync(path.join(d,"index.html"),page(p))}posts.sort((a,b)=>String(b.date).localeCompare(String(a.date)));fs.mkdirSync(path.join(OUT,"writing"),{recursive:true});fs.writeFileSync(path.join(OUT,"writing","index.json"),JSON.stringify(posts.map(({body,...p})=>p),null,2));const base="https://ubiquitous-pothos-a92d14.netlify.app";fs.writeFileSync(path.join(OUT,"robots.txt"),`User-agent: *\nAllow: /\nSitemap: ${base}/sitemap.xml\n`);fs.writeFileSync(path.join(OUT,"sitemap.xml"),`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[base+"/",...posts.map(p=>base+p.url)].map(u=>`<url><loc>${u}</loc></url>`).join("")}</urlset>`);console.log(`Built ${posts.length} posts`);
